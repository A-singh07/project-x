import React from 'react';
import styles from './statCards.module.css'

const StatCards = ({ image, heading, data, ...rest }) => {

  return (
    <div className={styles.cardContainer}>

    </div>
  )
}

export default StatCards
